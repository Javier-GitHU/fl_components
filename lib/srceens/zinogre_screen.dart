import 'package:flutter/material.dart';

// ignore: camel_case_types
class zinogre_screen extends StatelessWidget {
   
  const zinogre_screen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return  const Scaffold(
      body: Center(
        child: Text("Zinogre el perro del rayo"),
      ),
    );
  }
}